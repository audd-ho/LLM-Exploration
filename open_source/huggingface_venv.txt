pip install -U transformers --upgrade
pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

pip install numpy
pip install pandas

pip install huggingface-hub

pip install scikit-learn -- ? for training and fine tuning?

pip install -U "huggingface_hub[cli]" -- ? idk if need this to use huggingface-cli login
pip install accelerate -- ?? idk if need or only meta llama need?